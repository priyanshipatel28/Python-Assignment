# Write a Python program to generate random numbers using the random module.

import random

x=random.randrange(1,100)
print(x) # it will print random number between 1 to 100, everytine you run it. 